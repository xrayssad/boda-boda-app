name: Debug Secrets

on: push

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Check username
        run: |
          echo "DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}"
          echo "DOCKER_TOKEN length: ${#{{ secrets.DOCKER_TOKEN }}}"
